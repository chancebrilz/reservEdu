
import { miles } from 'reservedu/helpers/miles';
import { module, test } from 'qunit';

module('Unit | Helper | miles');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = miles([42]);
  assert.ok(result);
});

